{
  "name": "Binco-login-project",
  "version": "1.0.0",
  "private": true,
  "description": "Week 11 full-stack login assignment using React, Vite, Express, and Axios",
  "scripts": {
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently -n FRONTEND,BACKEND -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
